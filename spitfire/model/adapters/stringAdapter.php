<?php namespace spitfire\model\adapters;

/**
 * This adapter allows any data type that uses strings to interact directly
 * with the string fields inside the database. Nothing is done inside this 
 * adapter.
 */
class StringAdapter extends BaseAdapter
{
	
}
